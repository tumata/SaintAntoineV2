"""ButtonScope — live button-input voltage viewer (SPECS_BUTTONSCOPE.md).

Standalone diagnostic tool: samples a GPIO line, serves a live 10 s graph.
Read-only on the GPIO — never drives outputs.
"""

__version__ = "1.0.0"
